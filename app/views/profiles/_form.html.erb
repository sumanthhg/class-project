<%= form_for(@profile) do |f| %>
  <% if @profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@profile.errors.count, "error") %> prohibited this profile from being saved:</h2>

      <ul>
      <% @profile.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<%=  image_tag(@profile.cover.url, :style => 'width:150px;') if @profile.cover %>
  
  <div class="field">
    <%= f.label :bio %><br>
    <%= f.text_area :bio %>
  </div>
  
<div class="field">
  <%= f.label :cover %> 
  <%= f.file_field :cover %>
  </div>

  <div class="field">
<%= f.label :date_of_birth_using_datetime_picker %><br>

<%= f.text_field :date_of_birth, 'data-provide' => 'datepicker' %>
</div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
